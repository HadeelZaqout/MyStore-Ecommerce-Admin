@page
@using Microsoft.AspNetCore.Http.Features
@model TwoFactorAuthenticationModel
@{
    ViewData["Title"] = "Two-factor authentication (2FA)";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<partial name="_StatusMessage" for="StatusMessage" />
<h3 class="mb-1">@ViewData["Title"]</h3>
<p class="text-gray-600 mb-6">Strengthen account security with additional sign-in verification.</p>
@{
    var consentFeature = HttpContext.Features.Get<ITrackingConsentFeature>();
    @if (consentFeature?.CanTrack ?? true)
    {
        @if (Model.Is2faEnabled)
        {
            if (Model.RecoveryCodesLeft == 0)
            {
                <div class="alert alert-danger">
                    <strong>You have no recovery codes left.</strong>
                    <p>Recovery code management is not enabled in this project yet.</p>
                </div>
            }
            else if (Model.RecoveryCodesLeft == 1)
            {
                <div class="alert alert-danger">
                    <strong>You have 1 recovery code left.</strong>
                    <p>Consider enabling full recovery code management for better account safety.</p>
                </div>
            }
            else if (Model.RecoveryCodesLeft <= 3)
            {
                <div class="alert alert-warning">
                    <strong>You have @Model.RecoveryCodesLeft recovery codes left.</strong>
                    <p>Consider refreshing your recovery setup soon.</p>
                </div>
            }

            if (Model.IsMachineRemembered)
            {
                <form method="post" style="display: inline-block" class="me-2">
                    <button type="submit" class="btn btn-primary">Forget this browser</button>
                </form>
            }
            <div class="alert alert-info mt-4">2FA disable/reset actions are not scaffolded in this project yet.</div>
        }

        <h4 class="mt-6">Authenticator app</h4>
        @if (!Model.HasAuthenticator)
        {
            <div class="alert alert-warning">Authenticator setup page is not scaffolded yet.</div>
        }
        else
        {
            <div class="alert alert-info">Authenticator reset and setup pages are not scaffolded yet.</div>
        }
    }
    else
    {
        <div class="alert alert-danger">
            <strong>Privacy and cookie policy have not been accepted.</strong>
            <p>You must accept the policy before you can enable two factor authentication.</p>
        </div>
    }
}

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
